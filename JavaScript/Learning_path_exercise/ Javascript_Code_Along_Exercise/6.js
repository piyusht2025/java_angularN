// 6: Map and Set

// Create a Set called uniqueNumbers and add the numbers 5, 10, and 15 to it. Also, create a Map called userRoles where keys are user IDs (numbers) and values are roles (strings).

const a =new Set([5,10,15])
console.log(a);
const userRoles=new Map()
userRoles.set(5,'HR')
userRoles.set(10,'SDE')
userRoles.set(15,'SDE 2')

console.log(userRoles);